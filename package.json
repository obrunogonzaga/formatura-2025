{
  "name": "formatura-2025",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run prisma:generate --workspace apps/web",
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "start": "npm run start --workspace apps/web",
    "lint": "npm run lint --workspace apps/web",
    "prisma:generate": "npm run prisma:generate --workspace apps/web",
    "prisma:migrate": "npm run prisma:migrate --workspace apps/web",
    "prisma:deploy": "npm run prisma:deploy --workspace apps/web"
  }
}

